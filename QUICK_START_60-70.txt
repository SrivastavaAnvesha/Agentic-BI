"""
⚡ QUICK START - 60-70% Milestone Checklist
============================================

🎯 BEFORE RUNNING:
─────────────────

□ Python 3.8+ installed
□ PostgreSQL running with agentic_bi database
□ raw_sales_data table populated with data
□ .env file with GOOGLE_API_KEY

□ Required packages installed:
  pip install streamlit pandas plotly plotly-express
  pip install sqlalchemy psycopg2-binary python-dotenv google-genai


✅ INSTALLATION STEPS:
─────────────────────

1. Install missing packages (if any):
   
   pip install streamlit --upgrade
   pip install plotly plotly-express --upgrade

2. Verify PostgreSQL connection:
   
   psql -U postgres -d agentic_bi -c "SELECT COUNT(*) FROM raw_sales_data;"
   
   (Should return a number, not an error)

3. Check API key:
   
   Open .env file
   Verify: GOOGLE_API_KEY=your_actual_key_here
   (Not the placeholder)


🚀 RUN THE DASHBOARD:
────────────────────

Option 1 - Simple (Recommended):
   
   streamlit run app.py

Option 2 - With Logging:
   
   streamlit run app.py --logger.level=debug

Option 3 - Custom Port:
   
   streamlit run app.py --server.port 8502

✅ Dashboard will open automatically at: http://localhost:8501


📊 TEST THE FEATURES:
────────────────────

1. Quick Queries Section (⚡ fastest):
   
   ✓ Click "💰 Total Sales" button
   ✓ Click "📦 Total Orders" button
   ✓ Click "🏆 Top Category" button
   ✓ Click "🌍 Regional Sales" button
   
   Expected: All should show results in 2-5 seconds

2. Ask Your Own Question:
   
   ✓ Type: "What are the top 5 products?"
   ✓ Click "🚀 Analyze" button
   ✓ Check that metrics appear
   ✓ Verify chart displays (if applicable)

3. Sidebar Stats:
   
   ✓ Check "📊 Dashboard Stats"
   ✓ Should show live counts from database
   ✓ Numbers should match your data


🎨 VERIFY ALL VISUAL ELEMENTS:
──────────────────────────────

✓ Title appears: "📊 Agentic BI Dashboard"
✓ Subtitle: "Your AI-Powered Sales Analysis Engine..."
✓ Query input box with placeholder text
✓ "🚀 Analyze" button styled (blue/green)

✓ After running query:
  - ✅ Success message appears
  - 💰 Primary Value metric shows (with ₹)
  - 📈 Data Points metric shows
  - 📋 Data Results table displays

✓ Chart appears (if multi-row data):
  - 📊 Visualizations header visible
  - Bar chart with Viridis colors
  - Interactive (hover works)

✓ Sidebar shows:
  - 🛠️ Project Stack info
  - 📊 Dashboard Stats (live numbers)
  - 👨‍💼 About section


🔧 TROUBLESHOOTING:
──────────────────

❌ "ModuleNotFoundError: No module named 'plotly'"
→ Solution: pip install plotly --upgrade

❌ "Database connection refused"
→ Solution: Start PostgreSQL service
   Windows: Services > PostgreSQL > Start
   Or: net start postgresql-x64-15

❌ "No data returned / API error"
→ Solution: Check brain.py works first
   python brain.py

❌ "Charts not showing"
→ Solution: Your query must return multiple rows with numbers
   Try the quick queries which are guaranteed to work

❌ "Streamlit port already in use"
→ Solution: streamlit run app.py --server.port 8502

❌ "GOOGLE_API_KEY not found"
→ Solution: 
   1. Create/update .env file
   2. Add: GOOGLE_API_KEY=your_key_here
   3. Restart streamlit


📈 EXPECTED PERFORMANCE:
───────────────────────

✓ Dashboard loads:         1-2 seconds
✓ Quick query response:    3-5 seconds  
✓ Chart rendering:        < 1 second
✓ Total E2E time:         5-10 seconds

(Slower on first run due to Gemini API)


✨ NEW FEATURES CHECKLIST:
─────────────────────────

✅ Bar Chart Visualization
   - Automatic Plotly generation
   - Viridis color scale
   - Interactive hover tooltips
   - Responsive to data

✅ Professional Metrics
   - 2-column layout (60/40 split)
   - Left: Primary value with ₹ symbol
   - Right: Data point count
   - Beautiful gradient styling

✅ Error-Free DataFrame Handling
   - Detects single vs multi-column responses
   - Auto-converts database results to DF
   - Graceful fallbacks for non-numeric data
   - Detailed debug info in expandable section

✅ Quick Query Buttons
   - 4 pre-configured questions
   - One-click instant analysis
   - Pre-tested and guaranteed to work

✅ Live Dashboard Stats
   - Real-time record count
   - Real-time total sales amount
   - Direct database connection
   - Updates on page refresh

✅ Professional Styling
   - Dark gradient background
   - GitHub-inspired color scheme
   - Professional fonts & spacing
   - Responsive layout (works on all screen sizes)

✅ Sidebar Information
   - Project stack details
   - Live database statistics
   - Developer info
   - Progress milestone tracker


💡 TIPS & TRICKS:
────────────────

1. Fastest way to test: Click quick query buttons
   (No typing required, guaranteed results)

2. If chart doesn't appear: Your query returned single value
   (This is fine, metrics will still show)

3. For better charts: Ask questions that return multiple rows
   Examples:
   - "Sales by category" (multi-row)
   - "Sales by region" (multi-row)
   - "Products by sales" (multi-row)

4. Hover over chart bars: See exact values and labels

5. Expand "Technical Details": See raw database response
   (Useful for debugging)

6. Check sidebar stats: Verify your database has data


🎯 DEMO SCRIPT (4 minutes):
──────────────────────────

For a quick demo to others:

1. Start dashboard: streamlit run app.py (30 seconds)

2. Show quick queries: Click each button (60 seconds)
   Narrate: "One-click analysis, instant results"

3. Ask custom question: Type question + click analyze (60 seconds)
   Narrate: "AI understands natural language and generates SQL"

4. Point out features: Metrics, charts, sidebar stats (60 seconds)
   Narrate: "Professional styling, real-time data, interactive charts"

Result: Impressive demo in 4 minutes! 🎉


📞 IF NOTHING WORKS:
───────────────────

1. Verify PostgreSQL is running:
   → psql -U postgres -c "SELECT 1;"
   (Should return: 1)

2. Test brain.py separately:
   → python brain.py
   (Should run without errors)

3. Check Gemini API quota:
   → List available models: python list_models.py

4. Check for Python syntax errors:
   → python -m py_compile app.py
   (Should run without errors)

5. Check Streamlit installation:
   → streamlit --version
   (Should show version number)


🎉 YOU'RE READY!
───────────────

Your Agentic BI Dashboard is now:

✅ 60% Complete - Core functionality
✅ 65% Complete - Data Visualization
✅ 70% Complete - Professional UI

With:
- ✨ Beautiful Plotly charts
- 📊 Professional metrics layout
- ⚡ Quick query buttons
- 🎨 Gradient styling
- 📱 Responsive design
- 🔧 Error handling


NEXT PHASE (80% Milestone):
──────────────────────────

Add these features:
□ Multiple chart types (pie, line, heatmap)
□ Date range filters
□ Category dropdowns
□ Advanced analytics
□ CSV/PDF export
□ Performance caching


REMEMBER:
─────────

This is a 60-70% working prototype demo.
For 100% production-ready:
- Add authentication
- Add rate limiting per user
- Add error logging
- Add performance monitoring
- Add unit tests
- Add documentation


Good luck! 🚀

Questions? Check:
- DASHBOARD_UPDATE.md (detailed guide)
- ARCHITECTURE.txt (system design)
- app.py (code comments)

Happy analyzing! 📊✨
"""
